name: GitHub Actions Demo

run-name: ${{ github.actor }} is testing out GitHub Actions 🚀

on: [push]

jobs:
  Linter:
    runs-on: ubuntu-latest
    steps:
      name: Install
      script: 
        - run: yarn install
    steps:
      - run: yarn lint
