name: GitHub Actions Demo

run-name: ${{ github.actor }} is testing out GitHub Actions 🚀

on: [push]

jobs:
  Linter:
    runs-on: ubuntu-latest
    steps: 
      - name: Install yarn 
      - run: yarn install
    steps:
      - name: Run yarn lint
      - run: yarn lint
